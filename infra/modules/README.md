# Terraform Modules

Add shared Terraform modules here:

- `networking/` — VNet, subnets, NSGs, route tables
- `compute/` — VMs, VMSS, availability sets
- `identity/` — Managed identities, RBAC, Entra ID groups
- `monitoring/` — Log Analytics, Application Insights, alerts
